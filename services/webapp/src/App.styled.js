import Div100vh from 'react-div-100vh';
import styled from 'styled-components';

export const AppWrapper = styled(Div100vh)`
  display: flex;
  overflow: hidden;
  box-sizing: border-box;
  flex-direction: column;
`;
